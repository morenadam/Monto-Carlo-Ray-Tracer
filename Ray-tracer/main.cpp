#include <iostream>
#include <list>

struct Vertex {
    double x, y, z, w;
};

struct Direction {
    double x, y, z;
};

struct ColorDbI {
    double r, g, b;
};

struct Ray {
    Vertex startPoint;
    Vertex endPoint;
    List<Vertex>
};




int main() {
    std::cout << "Hello, World Test!" << std::endl;
    return 0;
}